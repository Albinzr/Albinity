version: 2
jobs: 
  build:
    working_dircetory: ~/temp
    docker: 
      - image: circleci/node:8
    steps:
      - checkout
      - run: pwd
      - run: ls
      - run: yarn install
      - run: npm test
