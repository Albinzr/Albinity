version: 2
jobs: 
  build:
    working_dircetory: ~/temp
    docker: 
      - image: circleci/node:8
    steps:
      - checkout
      - run: pwd
      - run: ls
      - run: cd server && ls &&  yarn install && npm test
 
